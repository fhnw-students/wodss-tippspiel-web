export class NewTip {

  public hostScore: number;
  public guestScore: number;

  constructor(hostScore: number, guestScore: number) {
    this.hostScore = hostScore;
    this.guestScore = guestScore;
  }

}
